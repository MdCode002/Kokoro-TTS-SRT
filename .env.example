API_KEY=change_me_super_secret_key_123
KOKORO_LANG_CODE=a
MODEL_DEVICE=cpu
# persistent = keep model in RAM (fast, uses ~2-3GB), on_demand = load/unload per request (slow first call, saves RAM)
MODEL_MODE=on_demand
